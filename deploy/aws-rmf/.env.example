# Copy this file to .env on the EC2 instance and adjust values there.
# Do not commit the real .env file.

FLEET_DOMAIN=fleet.wheelbot.tech
API_DOMAIN=api.fleet.wheelbot.tech

# Used by Let's Encrypt through Caddy automatic HTTPS.
ACME_EMAIL=admin@wheelbot.tech

POSTGRES_DB=rmf
POSTGRES_USER=rmf
POSTGRES_PASSWORD=change-me-before-deploy

TZ=Europe/Bucharest


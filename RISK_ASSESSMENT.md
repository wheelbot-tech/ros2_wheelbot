['front_right, rear_left]` but kinematics computes 4 wheel commands\n- Rear-right and front-left virtual joints still receive commands (set to 0)\n- Odometry averages only active wheels but kinematics uses all 4 positions\n- **Risk:** Incorrect odometry during rotation, steering commands to non-existent wheels\n- **Mitigation:** Create 2-wheel swerve kinematics variant or document limitation clearly\n\n### 4. Steering Command Scaling Logic Mismatch\n**Location:** `serial_hardware.cpp:259-285` (write) vs `swerve_drive_controller.cpp:477-502`\n- **Hardware layer:** Scales velocity based on steering error (`steering_scale` function, cos-based)\n- **Controller layer:** Also scales velocity based on steering error (cos-based, 30° threshold)\n- **Double scaling** occurs - velocity reduced twice for same steering error\n- **Risk:** Severely reduced mobility during steering transitions, unpredictable behavior\n- **Mitigation:** Remove scaling from one layer (prefer controller layer, it\'s more sophisticated)\n\n### 5. IMU Source Confusion\n**Location:** `serial_protocol.cpp:61-65` (is_chassis_imu_source), `serial_hardware.cpp:378-383`\n- Parses `IMU MASTER`, `IMU BASE`, `IMU CHASSIS` as chassis IMU\n- Also parses `IMUQ` (quaternion) from same sources\n- Priority: IMUQ > IMU (if quaternion seen, ignore non-quaternion)\n- **Risk:** If master sends both IMU and IMUQ, orientation may flip between sources\n- **Mitigation:** Enforce single IMU message type from master, document expected format\n\n## High Risks (High Impact, Medium Likelihood)\n\n### 6. Joint Name Mismatch Between URDF and Controller\n**Location:** `swerve_2bot_serial_controllers.yaml:22-30` vs `wheelbot_serial_swerve_2bot.ros2_control.xacro:27-77`\n- Controller expects: `virtual_front_left_wheel_joint`, `virtual_front_left_steering_joint`, etc.\n- URDF defines: Same names but ALL 4 corners (FL, FR, RL, RR)\n- Only FR and RL modules physically exist\n- **Risk:** Controller tries to command non-existent FL/RR joints, hardware interface returns 0 state\n- **Current behavior:** `active_wheels` disables odometry for missing wheels but commands still sent\n- **Mitigation:** Align controller config with physical modules, or add dummy hardware interfaces\n\n### 7. Namespace/Prefix Handling Inconsistency\n**Location:** `control_Layer_serial_launch.py:49-102` (robot_description), `serial_hardware.cpp:72-74`\n- `frame_prefix` applied to TF frames via `robot_state_publisher`\n- Joint names in `/joint_states` **not** prefixed (by design)\n- IMU `frame_id` constructed from `frame_prefix` + `imu_link`\n- **Risk:** If namespace changes but frame_prefix doesn\'t, TF tree breaks\n- **Mitigation:** Validate namespace/frame_prefix consistency at launch, add runtime checks\n\n### 8. No Velocity Command Rate Limiting / Ramp\n**Location:** `serial_hardware.cpp:269-285` (write), `swerve_drive_controller.cpp:466-475`\n- Hardware layer: No rate limiting on VEL commands\n- Controller layer: Velocity threshold clamping only (1.0 m/s)\n- **Risk:** Step changes in cmd_vel cause instantaneous wheel acceleration, mechanical stress\n- **Mitigation:** Add acceleration limits in controller or hardware layer\n\n### 9. Baudrate / Serial Port Hardcoded Defaults\n**Location:** `swerve_2bot_serial.launch.py:33-35`, `serial_hardware.cpp:72-73`\n- Default: `/dev/ttyACM0`, 115200 baud\n- No auto-detection of USB port (may be ACM0, ACM1, USB0, etc.)\n- **Risk:** Launch fails silently if port name changes, wrong baudrate = garbled data\n- **Mitigation:** Add udev rules for persistent symlink, validate serial communication on startup\n\n### 10. EKF Configuration Mismatch with Namespaced Topics\n**Location:** `ekf_swerve_imu.yaml:16, 29` vs `control_Layer_serial_launch.py:266-268`\n- EKF config uses `swerve_controller/odom` and `imu/data` (no namespace)\n- Launch file remaps `/tf` → `/robot_1/tf` but NOT odom/imu topics\n- **Risk:** EKF subscribes to global topics but robot publishes namespaced topics\n- **Current workaround:** Launch remaps TF only, EKF may not receive data in namespaced setup\n- **Mitigation:** Use topic remapping in EKF node or configure with namespace-aware topics\n\n## Medium Risks (Medium Impact, Medium Likelihood)\n\n### 11. Steering Zero-When-Stopped Logic Conflict\n**Location:** `serial_hardware.cpp:261-263, 272-273` (zero_steering_when_stopped), `swerve_drive_controller.cpp:513-519`\n- Hardware layer: Forces steering to 0 when robot stopped\n- Controller layer: Holds previous steering angle when stopped (`is_stop` branch)\n- **Conflict:** Hardware overrides controller\'s steering hold\n- **Risk:** Steering wheels snap to 0 on stop, then rotate on next move - mechanical wear\n- **Mitigation:** Unify behavior in one layer, disable in the other\n\n### 12. Active Modules Config Drift\n**Location:** Multiple files:\n- `swerve_2bot_serial.launch.py:36` - `active_modules` default `FR,RL`\n- `swerve_2bot_serial.urdf.xacro:9` - `active_modules` default `FR,RL`\n- `wheelbot_serial_swerve_2bot.ros2_control.xacro:13` - `active_modules` parameter\n- `serial_hardware.cpp:96-99` - Parses `active_modules` from hardware params\n- `swerve_2bot_serial_controllers.yaml:44` - `active_wheels: [front_right, rear_left]`\n- **Risk:** Inconsistent module lists across config files = undefined behavior\n- **Mitigation:** Single source of truth (e.g., launch argument → all configs)\n\n### 13. No Module Health Monitoring\n**Location:** `serial_hardware.cpp:356-393` (read_serial_lines)\n- Reads STATE lines but no timeout tracking per module\n- If module stops sending STATE, last state persists indefinitely\n- **Risk:** Stale position/velocity data used for control/odometry\n- **Mitigation:** Add per-module state timeout, flag stale data, trigger ESTOP\n\n### 14. Quaternion Normalization Without Validation\n**Location:** `serial_protocol.cpp:163-184` (parse_imuq_line)\n- Normalizes quaternion but doesn\'t check for reasonable values\n- Only checks `isfinite` and norm > 1e-6\n- **Risk:** Corrupted IMU data (e.g., NaN, extreme values) gets normalized and published\n- **Mitigation:** Add bounds checking on quaternion components, angular velocity, acceleration\n\n### 15. Twist Mux Stamped/Unstamped Dual Nodes\n**Location:** `twist_mux_launch.py:37-62`\n- Runs BOTH stamped and unstamped twist_mux nodes (conditionally)\n- Both subscribe to same input topics, publish to different outputs\n- **Risk:** Topic confusion, duplicate processing, unclear which output controller uses\n- **Mitigation:** Use single twist_mux with `use_stamped` parameter (supported in newer versions)\n\n## Low Risks (Low Impact / Low Likelihood)\n\n### 16. Steering Drive Mode Parameter String Comparison\n**Location:** `serial_hardware.cpp:91`\n```cpp\nsmooth_arc_steering_ = get_parameter(info_, "steering_drive_mode', 'strict_gate") == "smooth_arc', 'String comparison, typo in parameter value = silent fallback to strict_gate\n- **Mitigation:** Use enum or boolean parameter\n\n### 17. Hardcoded Wheel Positions in Kinematics\n**Location:** `swerve_drive_kinematics.cpp:27-30`\n- Wheel positions calculated from wheelbase/trackwidth only\n- No calibration offset per module\n- **Risk:** Manufacturing tolerances cause odometry drift\n- **Mitigation:** Add per-wheel position calibration parameters\n\n### 18. No Simulation Parity for Serial Hardware\n**Location:** `topic_based_ros2_control/` exists but not used for serial flow\n- No Gazebo/Isaac Sim equivalent of `WheelbotSerialHardware`\n- **Risk:** Cannot test control logic in simulation without hardware\n- **Mitigation:** Implement simulated hardware interface matching serial protocol\n\n### 19. JETSON_SHUTDOWN File Race Condition\n**Location:** `serial_hardware.cpp:505-533`\n- Opens file with `O_CREAT | O_TRUNC`, writes, closes\n- No file locking, multiple processes could interfere\n- **Mitigation:** Use atomic write (write to temp + rename) or lock file\n\n### 20. Missing Covariance Tuning\n**Location:** `serial_hardware.cpp:466-488` (IMU covariance), `swerve_2bot_serial_controllers.yaml:37-38`\n- Fixed covariance values, not tuned for actual sensor noise\n- **Risk:** EKF over/under-weights sensors, poor fusion\n- **Mitigation:** Calibrate covariances from real data, make configurable\n\n## Recommendations Priority Order\n\n| Priority | Action |\n|----------|--------|\n| P0 | Fix double steering velocity scaling (Risk #4) |\n| P0 | Align active wheels config across all files (Risk #12) |\n| P0 | Add per-module STATE timeout monitoring (Risk #13) |\n| P1 | Verify EKF topic remapping in namespaced launch (Risk #10) |\n| P1 | Unify steering-zero-when-stopped logic (Risk #11) |\n| P1 | Add ESTOP acknowledgment protocol (Risk #2) |\n| P2 | Add serial link watchdog (Risk #1) |\n| P2 | Implement velocity ramping/acceleration limits (Risk #8) |\n| P2 | Add udev rules for persistent serial port (Risk #9) |\n| P3 | Single twist_mux node configuration (Risk #15) |\n| P3 | Document 2-wheel swerve limitation (Risk #3) |\n| P4 | IMU covariance calibration (Risk #20) |\n| P4 | Add simulation hardware interface (Risk #18) |']